import { useEffect, useState } from "react";
import { ActivityIndicator, StyleSheet, Text, View } from "react-native";
import type { TaskScheduleResponse } from "@ewm/shared-types";
import { apiFetch } from "../../lib/api";
export default function CalendarScreen() { const [items, setItems] = useState<TaskScheduleResponse[]>([]); useEffect(() => { const from = new Date(); from.setHours(0, 0, 0, 0); const to = new Date(from); to.setDate(to.getDate() + 7); void apiFetch<TaskScheduleResponse[]>(`/schedule?from=${encodeURIComponent(from.toISOString())}&to=${encodeURIComponent(to.toISOString())}`).then(setItems); }, []); if (!items) return <View style={styles.center}><ActivityIndicator /></View>; return <View style={styles.container}><Text style={styles.title}>Calendar</Text>{items.length === 0 ? <Text style={styles.meta}>No scheduled work this week.</Text> : items.map((item) => <View key={item.id} style={styles.card}><Text style={styles.heading}>{item.taskTitle}</Text><Text style={styles.meta}>{new Date(item.scheduledStart).toLocaleString()} to {new Date(item.scheduledEnd).toLocaleTimeString()}</Text></View>)}</View>; }
const styles = StyleSheet.create({ container: { flex: 1, padding: 22, backgroundColor: "#f5f7f4" }, center: { flex: 1, justifyContent: "center", alignItems: "center" }, title: { fontSize: 30, fontWeight: "800", color: "#17324d", marginBottom: 20 }, card: { backgroundColor: "#fff", borderRadius: 12, padding: 16, marginBottom: 10 }, heading: { fontWeight: "800" }, meta: { color: "#68737d", marginTop: 6 } });
